{
  "name": "Mon container Codespace",
  "image" : "scalgary/selenium-env:latest",

  "containerEnv": {
    "YOUR_SECRET_EMAIL": "${localEnv:YOUR_SECRET_EMAIL}",
    "YOUR_SECRET_PASSWORD": "${localEnv:YOUR_SECRET_PASSWORD}",
    "YOUR_SECRET_LOGON_URL": "${localEnv:YOUR_SECRET_LOGON_URL}",
    "YOUR_SECRET_PLANNING_URL": "${localEnv:YOUR_SECRET_PLANNING_URL}",
    "YOUR_SECRET_LOGIN_URL": "${localEnv:YOUR_SECRET_LOGIN_URL}",
    "YOUR_SECRET_MY_NAME": "${localEnv:YOUR_SECRET_MY_NAME}",
    "YOUR_SECRET_APPOINTMENT": "${localEnv:YOUR_SECRET_APPOINTMENT}",
    "EMAIL_TO": "${localEnv:EMAIL_TO}",
    "EMAIL_FROM": "${localEnv:EMAIL_FROM}",
    "EMAIL_PASSWORD": "${localEnv:EMAIL_PASSWORD}",
    "CALDAV_USER": "${localEnv:CALDAV_USER}",
    "CALDAV_PASSWORD": "${localEnv:CALDAV_PASSWORD}"






  },
  "postCreateCommand": "bash .devcontainer/setup.sh 2>&1 | tee /tmp/setup_output.log ",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
         "ms-toolsai.jupyter"

      ],
      "settings": {
       "python.defaultInterpreterPath": "/usr/local/bin/python",
      "locale": "en"
   }
  }
},
  "remoteUser": "vscode"
}